So I am telling you just telling me how can we achieve this. So let's say we are giving a repository. So it has to run the repository in a server, so we'll get the UI. So once we send the code base to the LLM, it can understand how it can understand, visualize the content, all those things in the repository, and generate the test case for that. With the help of the Chromium and the screenshot, we can able to open the browser and touch and do the test case, and we can show the live of that. So is it possible to do or what?